export enum PaymentMethod {
  flutterwave = 'flutterwave',
  paystack = 'paystack',
  cash = 'cash',
}

export enum TransactionStatus {
  failed = 'failed',
  success = 'success',
  pending = 'pending',
}
export enum DiscountPaymentStatus {
  failed = 'failed',
  success = 'success',
  pending = 'pending',
}
